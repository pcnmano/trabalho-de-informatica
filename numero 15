#include <stdio.h>

int main()
{
    int nmr;
    
    printf("Informe o valor: ");
    scanf("%d", &nmr);
    
    if(nmr > 0)
    {
        printf("O valor é positivo\n");
    }
    else
    {
        printf("O valor é negativo\n");
    }
    return 0;
}
